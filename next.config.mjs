/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "unsplash.com", "assets.aceternity.com"], // Add the allowed image domains here
  },
};

export default nextConfig;
